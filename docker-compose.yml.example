services:
  web:
    image: course-notes-rag:latest
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
    ports:
      - 8501:8501
    dns:
      - 9.9.9.9
    volumes:
      - ./data:/app/data:z,ro
      - model:/app/model:z

volumes:
  model:
